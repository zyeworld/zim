{
  "name": "zim",
  "displayName": "zim",
  "description": "Invasive keybinds for visual editing",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "zim.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "zim.enterNormalMode",
        "title": "enterNormalMode"
      },
      {
        "command": "zim.enterInsertMode",
        "title": "enterInsertMode"
      }
    ],
    "keybindings": [
      {
        "key": "Escape",
        "command": "zim.escape",
        "when": "editorTextFocus && !inDebugRepl"
      },
      {
        "key": "Escape",
        "command": "notebook.cell.quitEdit",
        "when": "inputFocus && notebookEditorFocused && !editorHasSelection && !editorHoverVisible"
      },
      {
        "key": "ctrl+home",
        "command": "zim.ctrl+home",
        "when": "editorTextFocus && !inDebugRepl"
      },
      {
        "key": "Space",
        "command": "zim.space",
        "when": "editorTextFocus && !inDebugRepl"
      },
      {
        "key": "i",
        "command": "zim.i",
        "when": "editorTextFocus && !inDebugRepl"
      },
      {
        "key": "j",
        "command": "zim.j",
        "when": "editorTextFocus && !inDebugRepl"
      }
    ],
    "configuration":[
      {
        "title": "Zim",
        "type": "object",
        "properties": {}
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.100.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "eslint": "^9.25.1",
    "typescript": "^5.8.3",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.2"
  }
}
